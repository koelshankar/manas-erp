"use client";

import { Dashboard } from "@/components/dashboard/dashboard";

/** The role dashboard. Layout, widgets and figures all come from elsewhere. */
export default function DashboardPage() {
  return <Dashboard />;
}
